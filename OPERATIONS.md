# RRG Sector Atlas 운영 가이드

## 정상 발행 기준

주간 작업은 완료된 미국 거래 주차의 조정종가를 수집하고, 모든 시리즈의 최소 이력·중복 여부·양수 가격·동일 기준일 정렬을 검증합니다. 검증을 통과한 스냅샷만 `data` 브랜치에 보존하고, 정적 웹은 이 스냅샷만 게시합니다. 마지막 단계에서는 공개 페이지의 앱 셸과 `data/latest.json`의 스냅샷 ID를 확인하므로, 작업 성공은 데이터 발행과 공개 배포가 모두 확인되었음을 뜻합니다.

| 상태 | 의미 | 화면 동작 | 운영 조치 |
|---|---|---|---|
| `VERIFIED` | 최신 검증 스냅샷과 공개 배포가 확인됨 | 기준일과 이벤트를 정상 표시 | 별도 조치 없음 |
| `STALE` | 기준일이 10일을 초과함 | 마지막 정상 스냅샷과 신선도 경고를 함께 표시 | 주간 작업·데이터 공급자 상태를 확인 |
| 품질 게이트 실패 | 누락·정렬 불일치·중복·이력 부족 등 | 기존 정상 스냅샷 유지 | Actions 로그에서 실패 검사와 심볼 확인 |
| 공개 배포 검증 실패 | 웹 앱 또는 공개 JSON이 기대 ID와 불일치 | 새 스냅샷을 성공으로 표시하지 않음 | Pages 설정과 배포 작업 로그를 확인 |

## 정기 작업과 재실행

정기 스냅샷 작업은 매주 토요일 08:17 UTC에 실행됩니다. 수동으로 다시 실행할 때는 GitHub Actions의 **Weekly RRG snapshot** 워크플로에서 `Run workflow`를 사용하고, 실행 사유를 기록합니다. 작업은 이전 `latest.json`을 새 데이터로 덮어쓰기 전에 모든 계산·발행 검사를 마치므로, 실패 시 공개 화면에는 마지막 검증본이 유지됩니다.

## 점검 순서

배포나 데이터 문제가 의심되면 먼저 Actions의 최신 **Weekly RRG snapshot** 실행에서 실패 단계를 확인합니다. 수집 오류라면 Alpha Vantage API 키·속도 제한·일시 네트워크 오류를 점검합니다. 품질 오류라면 기록된 검사명과 심볼을 기준으로 원천 주차 및 조정종가를 확인합니다. 공개 배포 오류라면 Pages의 게시 원본이 **GitHub Actions**인지, 그리고 배포 후 공개 JSON의 `snapshot_id`가 작업에서 발행한 ID와 동일한지 확인합니다.

수정이 필요할 때에는 `main` 브랜치에서 수정 후 CI를 통과시키고, 정기 작업을 수동 실행해 새 스냅샷과 공개 화면을 함께 검증합니다. 데이터 스냅샷은 `data` 브랜치에 누적 보존되므로 문제가 확인된 최신본으로 롤백해야 할 경우에는 해당 스냅샷을 다시 `latest.json`으로 승격하는 별도 검토를 거친 뒤 재배포합니다.
