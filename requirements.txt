# Runtime (Streamlit app) — reader only. No yfinance, no DB driver:
# the app reads Supabase over REST (stdlib urllib) with the read-only anon key.
streamlit>=1.40,<2
plotly>=5.24,<6
pandas>=2.2,<3
numpy>=1.26,<3
